# Telegram Бот для продажи услуг по созданию ботов

Полнофункциональный Telegram бот с личным кабинетом, системой заявок и админ-панелью.

## 🚀 Возможности

### Для пользователей:
- 📋 Просмотр услуг и прайс-листа
- ✍️ Оформление заявок на разработку ботов
- 👤 Личный кабинет с историей заявок
- 💬 Общение с менеджером по заявке
- 📊 Отслеживание статуса заявки
- ⭐ Система оценок и отзывов

### Для администратора:
- 📊 Статистика по пользователям и заявкам
- 📋 Управление всеми заявками
- 🔄 Изменение статусов заявок
- 💬 Общение с клиентами
- 📝 Добавление комментариев к заявкам
- 👥 Просмотр всех пользователей
- 📢 Рассылка сообщений всем пользователям

## 📦 Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd telegram-bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка
Отредактируйте файл `config.py`:
- `BOT_TOKEN` - токен вашего бота от @BotFather
- `ADMIN_ID` - ваш Telegram User ID

## 🎮 Запуск

```bash
python bot.py
```

или

```bash
python3 bot.py
```

## 📁 Структура проекта

```
telegram-bot/
├── bot.py                 # Главный файл запуска бота
├── config.py             # Конфигурация и текстовые сообщения
├── database.py           # Работа с базой данных SQLite
├── keyboards.py          # Клавиатуры и кнопки
├── handlers_user.py      # Обработчики для пользователей
├── handlers_admin.py     # Обработчики для администратора
├── requirements.txt      # Зависимости Python
├── README.md            # Документация
└── bot_database.db      # База данных (создается автоматически)
```

## 🗄️ База данных

База данных SQLite создается автоматически при первом запуске бота.

### Таблицы:
- **users** - пользователи бота
- **orders** - заявки на услуги
- **messages** - сообщения по заявкам
- **reviews** - отзывы клиентов

## 🔧 Команды бота

### Для всех пользователей:
- `/start` - Запуск бота и главное меню
- `/help` - Помощь

### Для администратора:
- `/admin` - Доступ к админ-панели

## 📊 Статусы заявок

- 🆕 **new** - Новая заявка
- 🔄 **in_progress** - В работе
- ✅ **completed** - Завершена
- ❌ **cancelled** - Отменена

## 🔐 Безопасность

- Все данные хранятся локально в SQLite базе данных
- Доступ к админ-панели защищен проверкой User ID
- Рекомендуется хранить `config.py` в `.gitignore`

## 📝 Настройка текстов

Все текстовые сообщения и описания услуг находятся в файле `config.py`.
Вы можете легко изменить:
- Приветственное сообщение
- Описание услуг и цены
- Контактную информацию
- Информацию "О нас"

## 🎨 Кастомизация

### Изменение меню
Измените функции в `keyboards.py` для добавления или удаления кнопок.

### Добавление функционала
1. Создайте новые обработчики в `handlers_user.py` или `handlers_admin.py`
2. Зарегистрируйте их в соответствующей функции `register_*_handlers()`
3. При необходимости добавьте новые таблицы в `database.py`

## 🐛 Решение проблем

### Бот не запускается
- Проверьте правильность токена в `config.py`
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте логи на наличие ошибок

### База данных не создается
- Убедитесь, что у программы есть права на запись в текущую директорию
- Проверьте, что путь к БД в `config.py` указан корректно

### Админ-панель не работает
- Убедитесь, что ваш User ID правильно указан в `config.py`
- Используйте команду `/admin` для доступа к панели

## 📈 Рекомендации по развертыванию

### На сервере (Linux):
```bash
# Установка в фоновом режиме с помощью screen
screen -S telegram_bot
python3 bot.py
# Нажмите Ctrl+A, затем D для выхода из screen
```

### С использованием systemd:
Создайте файл `/etc/systemd/system/telegram-bot.service`:
```ini
[Unit]
Description=Telegram Bot Service
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 /path/to/bot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## 📞 Поддержка

При возникновении вопросов или проблем:
1. Проверьте логи бота
2. Убедитесь, что используете последнюю версию кода
3. Проверьте, что все зависимости установлены

## 📄 Лицензия

Этот проект создан для образовательных и коммерческих целей.

## 🔄 Обновления

Для обновления бота:
1. Сохраните файл `bot_database.db` (ваша база данных)
2. Обновите файлы бота
3. Перезапустите бот

## 💡 Идеи для улучшения

- Добавление платежных систем (ЮKassa, Stripe)
- Интеграция с CRM-системами
- Многоязычность
- Расширенная аналитика
- Автоматические уведомления
- Система скидок и промокодов
- Календарь для записи на консультации

---

**Приятного использования!** 🚀
